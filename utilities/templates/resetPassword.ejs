<!DOCTYPE html>
<html lang="en">
<head>
	<meta charset="UTF-8" />
	<meta name="viewport" content="width=device-width, initial-scale=1.0" />
	<title>Reset Password</title>
	<style>
		body {
			margin: 0;
			padding: 0;
			font-family: Arial, sans-serif;
			background-color: #f0f4f8;
			display: flex;
			align-items: center;
			justify-content: center;
			min-height: 100vh;
		}
		.container {
			background-color: #ffffff;
			padding: 40px 30px;
			border-radius: 10px;
			box-shadow: 0 0 15px rgba(0, 0, 0, 0.1);
			max-width: 500px;
			text-align: center;
		}
		.logo {
			max-height: 120px;
		}
		h1 {
			margin-bottom: 10px;
			color: #2b4f81;
		}
		p {
			font-size: 16px;
			line-height: 1.6;
			margin-bottom: 10px;
		}
		.input-field {
			width: 90%;
			padding: 10px;
			margin: 10px 0;
			border: 1px solid #ccc;
			border-radius: 5px;
			font-size: 14px;
		}
		.btn {
			background-color: #2b4f81;
			color: #fff;
			padding: 12px 24px;
			font-size: 16px;
			border: none;
			border-radius: 5px;
			cursor: pointer;
			transition: background-color 0.3s ease;
		}
		.btn:hover {
			background-color: #1f3b61;
		}
		.error {
			color: #b91c1c;
			font-size: 15px;
			margin-bottom: 20px;
		}
		a.retry-link {
			color: #b91c1c;
			text-decoration: underline;
			font-size: 14px;
		}
		footer {
			margin-top: 30px;
			font-size: 12px;
			color: #777;
		}
		.button {
			margin-top: 25px;
		}
		a.login-btn {
			background-color: #2b4f81;
			color: #fff;
			text-decoration: none;
			padding: 12px 24px;
			font-size: 16px;
			border-radius: 5px;
			display: inline-block;
			transition: background-color 0.3s ease;
		}
		a.login-btn:hover {
			background-color: #1f3b61;
		}
	</style>
</head>
<body>
	<div class="container">
		<img src="/images/logo.png" alt="SmartBook Logo" class="logo" />

		<% if (success === true) { %>
			<h3>Reset Your Password</h3>
			<p>Enter your new password below and click submit to update it.</p>

			<form method="POST" action="/user/reset-password?verificationToken=<%= verificationToken %>">
				<input type="password" name="password" placeholder="New Password" required class="input-field"/>
				<button type="submit" class="btn">Reset Password</button>
			</form>
		<% } else { %>
			<h1 class="error">Token Expired or Invalid</h1>
			<p class="error">The password reset link has expired or is invalid.</p>
			<p>Request a new password reset link</p>
			<div class="button">
				<a href="<%= loginUrl %>" class="login-btn" target="_blank">Go to Login</a>
			</div>
		<% } %>

		<footer>
			<p>&copy; 2025 SmartBook. All rights reserved.</p>
		</footer>
	</div>
</body>
</html>
